# ndvi-urban-expansion
# Python 3.10+
#
# Install with:
#   pip install -r requirements.txt
#
# Versions pinned to the environment used during development.
# These are the packages directly required by the pipeline.
# Transitive dependencies (affine, cligj, click, etc.) are installed
# automatically by pip.

rasterio==1.5.0
numpy==2.4.4
geopandas==1.1.3
shapely==2.1.2
scikit-learn==1.8.0
matplotlib==3.10.8
pandas==3.0.2
joblib==1.5.3
pyproj==3.7.2
